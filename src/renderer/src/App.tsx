import { AppLayout } from '@/components/layout/AppLayout'

/**
 * Main App Component
 *
 * Entry point for the Panoramic Game Creator application.
 * Renders the main application layout with toolbar, panels, and editor area.
 */

function App(): JSX.Element {
  return <AppLayout />
}

export default App
